#include "main.h"
/**
 * print_last_digit - calculates last digit
 * @c: value to be entered
 *
 * Return: last digit
 */
int print_last_digit(int c)
{
	int p = c % 10;

	if (p < 0);
		p *= -1

	_putchar(p + '0');

	return (0);
}
